version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"

  adminer:
    image: adminer:latest
    ports:
      - "8083:8080"
    depends_on:
      - db
